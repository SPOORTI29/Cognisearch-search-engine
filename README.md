# Cognisearch-search-engine
CogniSearch is a modern, lightweight, full-stack document search engine built to allow users to index and search textual information seamlessly. The project leverages a React-based frontend, a Python Flask backend, and a local SQLite database to provide real-time fuzzy search capabilities.
